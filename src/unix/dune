;; Test of the Unix library

(env
  (dev
    (flags (:standard -w -37))))

(test
 (name stm_tests)
 (modules stm_tests)
 (package multicoretests)
 (libraries unix qcheck-stm.sequential qcheck-stm.domain)
 (action (run %{test} --verbose))
)
