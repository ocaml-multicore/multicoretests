name: 32bit 5.2

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/common.yml
    with:
      compiler: 'ocaml-variants.5.2.0~alpha1+options,ocaml-option-32bit'
      timeout: 240
