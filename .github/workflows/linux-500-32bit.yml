name: 32bit 5.0.0

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/common.yml
    with:
      compiler: 'ocaml-variants.5.0.0+options,ocaml-option-32bit'
      timeout: 240
